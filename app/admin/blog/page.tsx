"use client";
import BlogManagerModal from "@/src/Features/blogmanage/component/BlogManagerModal";

export default function AdminBlogPage() {
  return <BlogManagerModal isOpen={true} onClose={() => {}} asPage={true} />;
}
